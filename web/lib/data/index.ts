/**
 * STUB DATA — TEMPORARY
 *
 * These files exist ONLY to keep the web build passing while pages
 * are incrementally rewired to the real API (Waves 2-4).
 *
 * DO NOT add real data here. Every page should be migrated to fetch
 * from the API client (web/lib/api-client.ts) per WAVE_STATUS.md.
 *
 * Once all pages are rewired, delete this entire directory.
 */
export {};
